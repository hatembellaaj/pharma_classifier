# Backend configuration
OPENAI_API_KEY=your-openai-api-key
# Optional: override the BDPM REST API base URL
API_MEDICAMENTS_BASE=https://fr.gouv.medicaments.rest/api/medicaments
# Optional: change where data is stored
DATA_DIR=data

# Frontend configuration (only used when running Vite locally)
# VITE_API_URL=http://localhost:8000
